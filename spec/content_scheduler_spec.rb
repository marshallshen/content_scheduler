require 'spec_helper'

describe ContentScheduler do
  it 'has a version number' do
    expect(ContentScheduler::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
